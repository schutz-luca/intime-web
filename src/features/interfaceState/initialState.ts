/**
 * IMPORTS
 */
import { IInterfaceState } from './index.d'

/**
 * I define the compact menu initial state.
 */
const initialState: IInterfaceState = {
  compactMenu: false,
  isDarkTheme: false
}

/**
 * EXPORTS
 */
export default initialState